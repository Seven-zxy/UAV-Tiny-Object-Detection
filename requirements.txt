# UAV-Tiny-Object-Detection — pinned dependencies
# Environment: Python 3.10, CUDA 11.8
#
# STEP 1 — install PyTorch (CUDA 11.8 build) from the PyTorch index:
#   pip install torch==2.3.0 torchvision==0.18.0 \
#       --index-url https://download.pytorch.org/whl/cu118
#
# STEP 2 — install the rest:
#   pip install -r requirements.txt

# (torch / torchvision listed for reference; install via STEP 1 above)
torch==2.3.0
torchvision==0.18.0

# Detection framework
ultralytics==8.4.37

# Data processing & numerics
numpy==1.24.4
pandas==2.0.3
PyYAML==6.0.3
scipy==1.10.1

# Image I/O
pillow==10.4.0
opencv-python==4.11.0.86

# Visualization
matplotlib==3.7.5

# Utilities
tqdm==4.67.3
